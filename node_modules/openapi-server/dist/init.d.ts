/**
 * Given an express instance:
 * - configures the router
 * - starts server
 * - returns the server
 */
/// <reference types="node" />
import { Application } from "express";
import { Server } from "net";
declare const init: (app: Application) => Promise<Server>;
export default init;
