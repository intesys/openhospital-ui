import { Router } from 'express';
import { OpenAPIV3 } from 'openapi-types';
declare const buildV3Routes: (router: Router, paths: Record<string, OpenAPIV3.PathItemObject>) => Router;
export default buildV3Routes;
