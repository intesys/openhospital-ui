/// <reference types="node" />
import { Application } from "express";
import { Server } from "net";
declare const _default: (app: Application, server: Server) => void;
/**
 * Restarts the server on file changes
 */
export default _default;
