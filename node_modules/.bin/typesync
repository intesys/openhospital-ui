#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../typesync/bin/typesync" "$@"
  ret=$?
else 
  node  "$basedir/../typesync/bin/typesync" "$@"
  ret=$?
fi
exit $ret
