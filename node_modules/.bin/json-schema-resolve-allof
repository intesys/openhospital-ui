#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../json-schema-resolve-allof/bin/json-schema-resolve-allof" "$@"
  ret=$?
else 
  node  "$basedir/../json-schema-resolve-allof/bin/json-schema-resolve-allof" "$@"
  ret=$?
fi
exit $ret
